# Example env defaults (safe to commit)
APP_ENV=dev
APP_DEBUG=1

# Placeholder defaults (do not store secrets here)
DATABASE_URL="mysql://db_user:DB_PASS@127.0.0.1:3306/db_name"
STELLAR_HORIZON_URL="https://horizon-testnet.stellar.org"
