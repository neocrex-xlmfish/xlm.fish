# .env.example - template only. DO NOT commit secrets to the repo.
APP_ENV=dev
APP_SECRET=changeme_replace_in_local_env
DATABASE_URL=mysql://dbuser:dbpass@127.0.0.1:3306/xlm_fish
MAILER_DSN=null://localhost

# Copy to .env.local or to server shared/.env.local and replace values per-environment
